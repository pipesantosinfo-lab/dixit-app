/* La portada del sitio. El componente vive en app/preview/SitioCompleto.tsx
   — se queda en esa carpeta porque de ahi cuelgan sus piezas (SpinImage y
   CoverflowCarousel) — pero ya no es una ruta aparte: antes /  redirigia a
   /preview y esa direccion quedaba a la vista en la barra del navegador. */
export { default } from './preview/SitioCompleto'
